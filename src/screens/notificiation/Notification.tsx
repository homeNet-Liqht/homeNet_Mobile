import React from 'react';
import { View, Text } from 'react-native';
import { ContainerComponent } from '../../components';

export default function Notification() {
  return (
    <ContainerComponent back title='Notifications'></ContainerComponent>
  );
}
